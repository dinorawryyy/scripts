-- DYNEX v560 - ANINDA TETİKLEME SÜRÜMÜ (DinoScripts UI ile)
local Player = game:GetService("Players").LocalPlayer
local PPS = game:GetService("ProximityPromptService")
local UIS = game:GetService("UserInputService")

-- DinoScripts UI Tasarımı
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "DynexStatus"
ScreenGui.Parent = Player:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false

-- Ana çerçeve
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 200, 0, 60)
MainFrame.Position = UDim2.new(0.05, 0, 0.05, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(5, 5, 5)
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

-- Yeşil outline
local Outline = Instance.new("UIStroke")
Outline.Color = Color3.fromRGB(0, 255, 0)
Outline.Thickness = 2
Outline.Parent = MainFrame

-- Köşe yuvarlatma
local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 8)
UICorner.Parent = MainFrame

-- Başlık etiketi
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -10, 0, 25)
Title.Position = UDim2.new(0, 5, 0, 5)
Title.BackgroundTransparency = 1
Title.Text = "Dino AutoGrab INSTANT E"
Title.TextColor3 = Color3.fromRGB(0, 255, 0)
Title.Font = Enum.Font.GothamBlack
Title.TextSize = 16
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = MainFrame

-- Durum etiketi
local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(1, -10, 0, 30)
StatusLabel.Position = UDim2.new(0, 5, 0, 30)
StatusLabel.BackgroundTransparency = 1
StatusLabel.TextColor3 = Color3.fromRGB(255, 50, 50)
StatusLabel.Font = Enum.Font.GothamBold
StatusLabel.TextSize = 12
StatusLabel.TextXAlignment = Enum.TextXAlignment.Left
StatusLabel.TextYAlignment = Enum.TextYAlignment.Top
StatusLabel.Text = "INSTANT E: OFF [Z]"
StatusLabel.Parent = MainFrame

-- Animasyonlu parçacıklar
local particles = {}
for i = 1, 5 do
    local particle = Instance.new("Frame")
    particle.Size = UDim2.new(0, math.random(2, 4), 0, math.random(2, 4))
    particle.BackgroundColor3 = Color3.fromRGB(0, math.random(200, 255), 0)
    particle.BorderSizePixel = 0
    particle.BackgroundTransparency = math.random(50, 80)/100
    particle.Parent = MainFrame

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(1, 0)
    corner.Parent = particle

    particles[i] = {
        frame = particle,
        speedX = math.random(-80, 80)/100,
        speedY = math.random(-80, 80)/100,
        posX = math.random() * 0.9 + 0.05,
        posY = math.random() * 0.9 + 0.05
    }
end

-- Parçacık animasyonu
spawn(function()
    while task.wait(0.03) do
        for _, p in ipairs(particles) do
            p.posX = p.posX + p.speedX * 0.01
            p.posY = p.posY + p.speedY * 0.01

            if p.posX < 0.02 then 
                p.posX = 0.02 
                p.speedX = math.abs(p.speedX) 
            end
            
            if p.posX > 0.98 then 
                p.posX = 0.98 
                p.speedX = -math.abs(p.speedX) 
            end
            
            if p.posY < 0.02 then 
                p.posY = 0.02 
                p.speedY = math.abs(p.speedY) 
            end
            
            if p.posY > 0.98 then 
                p.posY = 0.98 
                p.speedY = -math.abs(p.speedY) 
            end

            p.frame.Position = UDim2.new(p.posX, -p.frame.AbsoluteSize.X/2, p.posY, -p.frame.AbsoluteSize.Y/2)
        end
    end
end)

-- [[ MANTIK ]] --
local IsActive = false

local function toggle()
    IsActive = not IsActive
    StatusLabel.Text = IsActive and "INSTANT E: ACTIVE" or "INSTANT E: OFF [Z]"
    StatusLabel.TextColor3 = IsActive and Color3.fromRGB(0, 255, 150) or Color3.fromRGB(255, 50, 50)
    Outline.Color = StatusLabel.TextColor3
    
    -- Parçacık renklerini değiştir
    for _, p in ipairs(particles) do
        if IsActive then
            p.frame.BackgroundColor3 = Color3.fromRGB(math.random(150, 255), math.random(200, 255), 0)
        else
            p.frame.BackgroundColor3 = Color3.fromRGB(0, math.random(200, 255), 0)
        end
    end
end

UIS.InputBegan:Connect(function(i, p)
    if not p and i.KeyCode == Enum.KeyCode.Z then
        toggle()
    end
end)

-- [] TETİKLEME SİSTEMİ
PPS.PromptButtonHoldBegan:Connect(function(prompt)
    if IsActive then
        prompt.HoldDuration = 0
        task.spawn(function()
            prompt:InputHoldBegin()
            prompt:InputHoldEnd()
            if fireproximityprompt then 
                fireproximityprompt(prompt) 
            end
        end)
    end
end)

PPS.PromptShown:Connect(function(prompt)
    if IsActive then
        prompt.HoldDuration = 0
    end
end)

print("[Dino AutoGrab] Instant E tetikleyici yüklendi!")
print("[Dino AutoGrab] Z tuşu ile aç/kapat")
print("[DINO UI] Animasyonlu arayüz aktif")
