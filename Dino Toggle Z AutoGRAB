-- DinoScripts Auto Grab Ultimate
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local Workspace = game:GetService("Workspace")
local ProximityPromptService = game:GetService("ProximityPromptService")
local HttpService = game:GetService("HttpService")

-- Platform detection
local isMobile = UserInputService.TouchEnabled
local platform = isMobile and "Mobile" or "PC"

-- Whitelist system
local WhitelistData = {
    Owner = "PetCenneti_X",
    Whitelisted = {"PetCenneti_X", "Dino", "Admin"}
}

-- Load saved whitelist
local function loadWhitelist()
    local success, data = pcall(function()
        if readfile then
            local saved = readfile("DinoScripts_Whitelist.json")
            return HttpService:JSONDecode(saved)
        end
    end)
    return success and data or WhitelistData
end

-- Save whitelist
local function saveWhitelist()
    pcall(function()
        if writefile then
            writefile("DinoScripts_Whitelist.json", HttpService:JSONEncode(WhitelistData))
        end
    end)
end

-- Initialize whitelist
WhitelistData = loadWhitelist()

-- Check if player is whitelisted
local function isWhitelisted(playerName)
    for _, name in ipairs(WhitelistData.Whitelisted) do
        if name:lower() == playerName:lower() then
            return true
        end
    end
    return false
end

-- Check if current player is owner
local isOwner = LocalPlayer.Name:lower() == WhitelistData.Owner:lower()
local isAllowed = isWhitelisted(LocalPlayer.Name)

-- UI Creation
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "DinoScriptsAutoGrabUI"
ScreenGui.Parent = game.CoreGui

-- Main UI Frame
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 350, 0, 250)
MainFrame.Position = UDim2.new(0.5, -175, 0.5, -125)
MainFrame.BackgroundColor3 = Color3.fromRGB(5, 5, 5)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.ClipsDescendants = true
MainFrame.Parent = ScreenGui

-- Green Outline
local Outline = Instance.new("UIStroke")
Outline.Name = "Outline"
Outline.Color = isAllowed and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 50, 50)
Outline.Thickness = 2
Outline.Parent = MainFrame

-- Corner rounding
local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 15)
UICorner.Parent = MainFrame

-- Title
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -20, 0, 45)
Title.Position = UDim2.new(0, 10, 0, 10)
Title.BackgroundTransparency = 1
Title.Text = "DINO AUTOGrab"
Title.TextColor3 = isAllowed and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 50, 50)
Title.Font = Enum.Font.GothamBlack
Title.TextSize = 20
Title.Parent = MainFrame

-- Account status
local AccountStatus = Instance.new("TextLabel")
AccountStatus.Size = UDim2.new(1, -20, 0, 20)
AccountStatus.Position = UDim2.new(0, 10, 0, 55)
AccountStatus.BackgroundTransparency = 1
AccountStatus.Text = isOwner and "OWNER ACCOUNT" or (isAllowed and "WHITELISTED" or "NOT WHITELISTED")
AccountStatus.TextColor3 = isOwner and Color3.fromRGB(255, 200, 0) or (isAllowed and Color3.fromRGB(0, 255, 150) or Color3.fromRGB(255, 50, 50))
AccountStatus.Font = Enum.Font.GothamBold
AccountStatus.TextSize = 12
AccountStatus.TextXAlignment = Enum.TextXAlignment.Right
AccountStatus.Parent = MainFrame

-- Made by Dino text
local MadeByLabel = Instance.new("TextLabel")
MadeByLabel.Size = UDim2.new(1, -20, 0, 18)
MadeByLabel.Position = UDim2.new(0, 10, 0, 75)
MadeByLabel.BackgroundTransparency = 1
MadeByLabel.Text = "made by Dino"
MadeByLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
MadeByLabel.Font = Enum.Font.GothamBold
MadeByLabel.TextSize = 11
MadeByLabel.TextXAlignment = Enum.TextXAlignment.Right
MadeByLabel.Parent = MainFrame

-- Status Container
local StatusContainer = Instance.new("Frame")
StatusContainer.Size = UDim2.new(1, -20, 0, 50)
StatusContainer.Position = UDim2.new(0, 10, 0, 95)
StatusContainer.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
StatusContainer.Parent = MainFrame

local StatusContainerCorner = Instance.new("UICorner")
StatusContainerCorner.CornerRadius = UDim.new(0, 8)
StatusContainerCorner.Parent = StatusContainer

local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(1, -10, 1, -10)
StatusLabel.Position = UDim2.new(0, 5, 0, 5)
StatusLabel.BackgroundTransparency = 1
StatusLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
StatusLabel.Font = Enum.Font.Gotham
StatusLabel.TextSize = 13
StatusLabel.TextWrapped = true
StatusLabel.TextXAlignment = Enum.TextXAlignment.Left
StatusLabel.TextYAlignment = Enum.TextYAlignment.Top
StatusLabel.Text = isAllowed and "Status: Ready - Platform: "..platform.."\nAuto Grab Active - Press Z to toggle" or "Status: NOT WHITELISTED\nContact PetCenneti_X"
StatusLabel.Parent = StatusContainer

-- Execute Button (Toggle Auto Grab)
local ExecuteButton = Instance.new("TextButton")
ExecuteButton.Size = UDim2.new(1, -20, 0, 45)
ExecuteButton.Position = UDim2.new(0, 10, 0, 150)
ExecuteButton.BackgroundColor3 = isAllowed and Color3.fromRGB(0, 100, 0) or Color3.fromRGB(100, 0, 0)
ExecuteButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ExecuteButton.Font = Enum.Font.GothamBold
ExecuteButton.TextSize = 18
ExecuteButton.Text = isAllowed and "⚡ TOGGLE AUTOGrab ⚡" or "NOT WHITELISTED"
ExecuteButton.Parent = MainFrame

local ButtonCorner = Instance.new("UICorner")
ButtonCorner.CornerRadius = UDim.new(0, 10)
ButtonCorner.Parent = ExecuteButton

-- Info Text
local InfoText = Instance.new("TextLabel")
InfoText.Size = UDim2.new(1, -20, 0, 40)
InfoText.Position = UDim2.new(0, 10, 0, 200)
InfoText.BackgroundTransparency = 1
InfoText.TextColor3 = Color3.fromRGB(180, 180, 180)
InfoText.Font = Enum.Font.Gotham
InfoText.TextSize = 11
InfoText.TextWrapped = true
InfoText.TextXAlignment = Enum.TextXAlignment.Left
InfoText.TextYAlignment = Enum.TextYAlignment.Top
InfoText.Text = "Auto Grab everything in proximity\nInstant E activation\nWorks on any proximity prompts"
InfoText.Parent = MainFrame

-- Simple particle effect
local particles = {}
for i = 1, 8 do
    local particle = Instance.new("Frame")
    particle.Size = UDim2.new(0, math.random(2, 4), 0, math.random(2, 4))
    particle.BackgroundColor3 = isAllowed and Color3.fromRGB(0, math.random(200, 255), 0) or Color3.fromRGB(math.random(200, 255), 50, 50)
    particle.BorderSizePixel = 0
    particle.BackgroundTransparency = math.random(50, 80)/100
    particle.Parent = MainFrame
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(1, 0)
    corner.Parent = particle
    
    particles[i] = {
        frame = particle,
        speedX = math.random(-100, 100)/100,
        speedY = math.random(-100, 100)/100,
        posX = math.random() * 0.9 + 0.05,
        posY = math.random() * 0.9 + 0.05
    }
end

-- Animate particles
spawn(function()
    while true do
        for _, p in ipairs(particles) do
            p.posX = p.posX + p.speedX * 0.01
            p.posY = p.posY + p.speedY * 0.01
            
            if p.posX < 0.02 then p.posX = 0.02 p.speedX = math.abs(p.speedX) end
            if p.posX > 0.98 then p.posX = 0.98 p.speedX = -math.abs(p.speedX) end
            if p.posY < 0.02 then p.posY = 0.02 p.speedY = math.abs(p.speedY) end
            if p.posY > 0.98 then p.posY = 0.98 p.speedY = -math.abs(p.speedY) end
            
            p.frame.Position = UDim2.new(p.posX, -p.frame.AbsoluteSize.X/2, p.posY, -p.frame.AbsoluteSize.Y/2)
        end
        wait(0.03)
    end
end)

-- UI toggle with CTRL (PC only)
local uiVisible = true
if not isMobile then
    UserInputService.InputBegan:Connect(function(input)
        if input.KeyCode == Enum.KeyCode.LeftControl or input.KeyCode == Enum.KeyCode.RightControl then
            uiVisible = not uiVisible
            MainFrame.Visible = uiVisible
        end
    end)
end

-- Whitelist UI (only for owner PetCenneti_X)
if isOwner then
    local WhitelistFrame = Instance.new("Frame")
    WhitelistFrame.Size = UDim2.new(0, 300, 0, 400)
    WhitelistFrame.Position = UDim2.new(0.5, -150, 0.5, -200)
    WhitelistFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
    WhitelistFrame.BorderSizePixel = 0
    WhitelistFrame.ClipsDescendants = true
    WhitelistFrame.Active = true
    WhitelistFrame.Draggable = true
    WhitelistFrame.Visible = false
    WhitelistFrame.Parent = ScreenGui

    local WhitelistOutline = Instance.new("UIStroke")
    WhitelistOutline.Color = Color3.fromRGB(0, 255, 0)
    WhitelistOutline.Thickness = 2
    WhitelistOutline.Parent = WhitelistFrame

    local WhitelistCorner = Instance.new("UICorner")
    WhitelistCorner.CornerRadius = UDim.new(0, 10)
    WhitelistCorner.Parent = WhitelistFrame

    -- Whitelist title
    local WhitelistTitle = Instance.new("TextLabel")
    WhitelistTitle.Size = UDim2.new(1, -20, 0, 40)
    WhitelistTitle.Position = UDim2.new(0, 10, 0, 10)
    WhitelistTitle.BackgroundTransparency = 1
    WhitelistTitle.Text = "WHITELIST MANAGER"
    WhitelistTitle.TextColor3 = Color3.fromRGB(0, 255, 0)
    WhitelistTitle.Font = Enum.Font.GothamBlack
    WhitelistTitle.TextSize = 20
    WhitelistTitle.Parent = WhitelistFrame

    -- Current players list
    local PlayersLabel = Instance.new("TextLabel")
    PlayersLabel.Size = UDim2.new(1, -20, 0, 30)
    PlayersLabel.Position = UDim2.new(0, 10, 0, 60)
    PlayersLabel.BackgroundTransparency = 1
    PlayersLabel.Text = "Online Players:"
    PlayersLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
    PlayersLabel.Font = Enum.Font.GothamBold
    PlayersLabel.TextSize = 14
    PlayersLabel.TextXAlignment = Enum.TextXAlignment.Left
    PlayersLabel.Parent = WhitelistFrame

    -- Players scroll frame
    local PlayersScroll = Instance.new("ScrollingFrame")
    PlayersScroll.Size = UDim2.new(1, -20, 0, 150)
    PlayersScroll.Position = UDim2.new(0, 10, 0, 95)
    PlayersScroll.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    PlayersScroll.BorderSizePixel = 0
    PlayersScroll.ScrollBarThickness = 5
    PlayersScroll.Parent = WhitelistFrame

    local PlayersScrollCorner = Instance.new("UICorner")
    PlayersScrollCorner.CornerRadius = UDim.new(0, 5)
    PlayersScrollCorner.Parent = PlayersScroll

    local PlayersLayout = Instance.new("UIListLayout")
    PlayersLayout.Padding = UDim.new(0, 5)
    PlayersLayout.Parent = PlayersScroll

    -- Whitelisted players list
    local WhitelistLabel = Instance.new("TextLabel")
    WhitelistLabel.Size = UDim2.new(1, -20, 0, 30)
    WhitelistLabel.Position = UDim2.new(0, 10, 0, 260)
    WhitelistLabel.BackgroundTransparency = 1
    WhitelistLabel.Text = "Whitelisted Players:"
    WhitelistLabel.TextColor3 = Color3.fromRGB(0, 255, 150)
    WhitelistLabel.Font = Enum.Font.GothamBold
    WhitelistLabel.TextSize = 14
    WhitelistLabel.TextXAlignment = Enum.TextXAlignment.Left
    WhitelistLabel.Parent = WhitelistFrame

    -- Whitelist scroll frame
    local WhitelistScroll = Instance.new("ScrollingFrame")
    WhitelistScroll.Size = UDim2.new(1, -20, 0, 80)
    WhitelistScroll.Position = UDim2.new(0, 10, 0, 295)
    WhitelistScroll.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    WhitelistScroll.BorderSizePixel = 0
    WhitelistScroll.ScrollBarThickness = 5
    WhitelistScroll.Parent = WhitelistFrame

    local WhitelistScrollCorner = Instance.new("UICorner")
    WhitelistScrollCorner.CornerRadius = UDim.new(0, 5)
    WhitelistScrollCorner.Parent = WhitelistScroll

    local WhitelistLayout = Instance.new("UIListLayout")
    WhitelistLayout.Padding = UDim2.new(0, 0, 0, 5)
    WhitelistLayout.Parent = WhitelistScroll

    -- Add player input
    local AddInput = Instance.new("TextBox")
    AddInput.Size = UDim2.new(1, -20, 0, 35)
    AddInput.Position = UDim2.new(0, 10, 0, 385)
    AddInput.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    AddInput.TextColor3 = Color3.fromRGB(255, 255, 255)
    AddInput.Font = Enum.Font.Gotham
    AddInput.TextSize = 14
    AddInput.PlaceholderText = "Enter username to add..."
    AddInput.PlaceholderColor3 = Color3.fromRGB(150, 150, 150)
    AddInput.Text = ""
    AddInput.Parent = WhitelistFrame

    local AddInputCorner = Instance.new("UICorner")
    AddInputCorner.CornerRadius = UDim.new(0, 5)
    AddInputCorner.Parent = AddInput

    -- Add button
    local AddButton = Instance.new("TextButton")
    AddButton.Size = UDim2.new(0.48, -5, 0, 30)
    AddButton.Position = UDim2.new(0, 10, 0, 430)
    AddButton.BackgroundColor3 = Color3.fromRGB(0, 100, 0)
    AddButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    AddButton.Font = Enum.Font.GothamBold
    AddButton.TextSize = 14
    AddButton.Text = "ADD"
    AddButton.Parent = WhitelistFrame

    local AddButtonCorner = Instance.new("UICorner")
    AddButtonCorner.CornerRadius = UDim.new(0, 5)
    AddButtonCorner.Parent = AddButton

    -- Remove button
    local RemoveButton = Instance.new("TextButton")
    RemoveButton.Size = UDim2.new(0.48, -5, 0, 30)
    RemoveButton.Position = UDim2.new(0.52, 0, 0, 430)
    RemoveButton.BackgroundColor3 = Color3.fromRGB(100, 0, 0)
    RemoveButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    RemoveButton.Font = Enum.Font.GothamBold
    RemoveButton.TextSize = 14
    RemoveButton.Text = "REMOVE"
    RemoveButton.Parent = WhitelistFrame

    local RemoveButtonCorner = Instance.new("UICorner")
    RemoveButtonCorner.CornerRadius = UDim.new(0, 5)
    RemoveButtonCorner.Parent = RemoveButton

    -- Close button
    local CloseButton = Instance.new("TextButton")
    CloseButton.Size = UDim2.new(1, -20, 0, 30)
    CloseButton.Position = UDim2.new(0, 10, 0, 470)
    CloseButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    CloseButton.Font = Enum.Font.GothamBold
    CloseButton.TextSize = 14
    CloseButton.Text = "CLOSE"
    CloseButton.Parent = WhitelistFrame

    local CloseButtonCorner = Instance.new("UICorner")
    CloseButtonCorner.CornerRadius = UDim.new(0, 5)
    CloseButtonCorner.Parent = CloseButton

    -- Update player lists
    local function updatePlayerLists()
        -- Clear existing lists
        for _, child in ipairs(PlayersScroll:GetChildren()) do
            if child:IsA("TextButton") or child:IsA("TextLabel") then
                child:Destroy()
            end
        end
        
        for _, child in ipairs(WhitelistScroll:GetChildren()) do
            if child:IsA("TextButton") or child:IsA("TextLabel") then
                child:Destroy()
            end
        end
        
        -- Add online players
        local players = game:GetService("Players"):GetPlayers()
        for _, player in ipairs(players) do
            if player.Name ~= LocalPlayer.Name then
                local playerButton = Instance.new("TextButton")
                playerButton.Size = UDim2.new(1, 0, 0, 30)
                playerButton.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
                playerButton.TextColor3 = Color3.fromRGB(255, 255, 255)
                playerButton.Font = Enum.Font.Gotham
                playerButton.TextSize = 12
                playerButton.Text = player.Name
                playerButton.AutoButtonColor = false
                playerButton.Parent = PlayersScroll
                
                local playerButtonCorner = Instance.new("UICorner")
                playerButtonCorner.CornerRadius = UDim.new(0, 3)
                playerButtonCorner.Parent = playerButton
                
                playerButton.MouseButton1Click:Connect(function()
                    AddInput.Text = player.Name
                end)
            end
        end
        
        -- Add whitelisted players
        for _, whitelistedName in ipairs(WhitelistData.Whitelisted) do
            local whitelistButton = Instance.new("TextButton")
            whitelistButton.Size = UDim2.new(1, 0, 0, 30)
            whitelistButton.BackgroundColor3 = Color3.fromRGB(0, 40, 0)
            whitelistButton.TextColor3 = Color3.fromRGB(255, 255, 255)
            whitelistButton.Font = Enum.Font.GothamBold
            whitelistButton.TextSize = 12
            whitelistButton.Text = whitelistedName
            whitelistButton.AutoButtonColor = false
            whitelistButton.Parent = WhitelistScroll
            
            local whitelistButtonCorner = Instance.new("UICorner")
            whitelistButtonCorner.CornerRadius = UDim.new(0, 3)
            whitelistButtonCorner.Parent = whitelistButton
            
            whitelistButton.MouseButton1Click:Connect(function()
                AddInput.Text = whitelistedName
            end)
        end
        
        -- Update scroll canvas sizes
        PlayersScroll.CanvasSize = UDim2.new(0, 0, 0, PlayersLayout.AbsoluteContentSize.Y)
        WhitelistScroll.CanvasSize = UDim2.new(0, 0, 0, WhitelistLayout.AbsoluteContentSize.Y)
    end
    
    -- Add player to whitelist
    AddButton.MouseButton1Click:Connect(function()
        local playerName = AddInput.Text:gsub("%s+", "")
        if playerName == "" then return end
        
        -- Check if already whitelisted
        for _, name in ipairs(WhitelistData.Whitelisted) do
            if name:lower() == playerName:lower() then
                AddInput.Text = "Already whitelisted!"
                task.wait(1)
                AddInput.Text = ""
                return
            end
        end
        
        -- Add to whitelist
        table.insert(WhitelistData.Whitelisted, playerName)
        saveWhitelist()
        updatePlayerLists()
        AddInput.Text = "Added "..playerName
        task.wait(1)
        AddInput.Text = ""
    end)
    
    -- Remove player from whitelist
    RemoveButton.MouseButton1Click:Connect(function()
        local playerName = AddInput.Text:gsub("%s+", "")
        if playerName == "" then return end
        
        -- Find and remove from whitelist
        for i, name in ipairs(WhitelistData.Whitelisted) do
            if name:lower() == playerName:lower() then
                table.remove(WhitelistData.Whitelisted, i)
                saveWhitelist()
                updatePlayerLists()
                AddInput.Text = "Removed "..playerName
                task.wait(1)
                AddInput.Text = ""
                return
            end
        end
        
        AddInput.Text = "Not in whitelist!"
        task.wait(1)
        AddInput.Text = ""
    end)
    
    -- Close whitelist manager
    CloseButton.MouseButton1Click:Connect(function()
        WhitelistFrame.Visible = false
    end)
    
    -- Toggle whitelist manager with Y key
    UserInputService.InputBegan:Connect(function(input, processed)
        if not processed and input.KeyCode == Enum.KeyCode.Y then
            WhitelistFrame.Visible = not WhitelistFrame.Visible
            if WhitelistFrame.Visible then
                updatePlayerLists()
            end
        end
    end)
    
    -- Initial update
    updatePlayerLists()
end

-- Auto Grab Logic
local autoGrabEnabled = false

-- Function to grab everything instantly
local function grabAllProximity()
    if not autoGrabEnabled or not isAllowed then return end
    
    -- Grab all proximity prompts
    for _, obj in pairs(Workspace:GetDescendants()) do
        if obj:IsA("ProximityPrompt") then
            -- Instant grab
            pcall(function()
                if fireproximityprompt then 
                    fireproximityprompt(obj) 
                else
                    obj.HoldDuration = 0
                    obj:InputHoldBegin()
                    obj:InputHoldEnd()
                end
            end)
        end
        
        -- Also grab any interactable parts/models
        if obj:IsA("Part") or obj:IsA("Model") then
            local nameLower = obj.Name:lower()
            if nameLower:find("item") or nameLower:find("collect") or nameLower:find("pickup") or nameLower:find("coin") or nameLower:find("money") then
                -- Try to interact with it
                pcall(function()
                    local char = LocalPlayer.Character
                    if char then
                        local rootPart = char:FindFirstChild("HumanoidRootPart")
                        if rootPart then
                            rootPart.CFrame = CFrame.new(obj.Position + Vector3.new(0, 3, 0))
                            wait(0.1)
                            VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.E, false, nil)
                            wait(0.05)
                            VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.E, false, nil)
                        end
                    end
                end)
            end
        end
    end
    
    -- Look for GUI items to grab
    for _, gui in pairs({LocalPlayer.PlayerGui}) do
        for _, obj in pairs(gui:GetDescendants()) do
            if obj:IsA("TextButton") and obj.Text then
                local textLower = obj.Text:lower()
                if textLower:find("collect") or textLower:find("grab") or textLower:find("pickup") or textLower:find("take") then
                    pcall(function()
                        VirtualInputManager:SendMouseButtonEvent(
                            obj.AbsolutePosition.X + obj.AbsoluteSize.X/2,
                            obj.AbsolutePosition.Y + obj.AbsoluteSize.Y/2,
                            0,
                            true,
                            Workspace.CurrentCamera,
                            1
                        )
                        wait(0.05)
                        VirtualInputManager:SendMouseButtonEvent(
                            obj.AbsolutePosition.X + obj.AbsoluteSize.X/2,
                            obj.AbsolutePosition.Y + obj.AbsoluteSize.Y/2,
                            0,
                            false,
                            Workspace.CurrentCamera,
                            1
                        )
                    end)
                end
            end
        end
    end
end

-- Main Auto Grab toggle function
ExecuteButton.MouseButton1Click:Connect(function()
    if not isAllowed then
        StatusLabel.Text = "NOT WHITELISTED\nContact PetCenneti_X"
        ExecuteButton.BackgroundColor3 = Color3.fromRGB(100, 0, 0)
        ExecuteButton.Text = "NOT WHITELISTED"
        return
    end
    
    autoGrabEnabled = not autoGrabEnabled
    
    if autoGrabEnabled then
        ExecuteButton.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
        ExecuteButton.Text = "⚡ AUTOGrab ON ⚡"
        StatusLabel.Text = "Status: Auto Grab ACTIVE\nGrabbing everything automatically"
        Outline.Color = Color3.fromRGB(0, 255, 150)
        
        -- Start auto grab loop
        spawn(function()
            while autoGrabEnabled and isAllowed do
                grabAllProximity()
                wait(0.3) -- Grab every 0.3 seconds
            end
        end)
        
        -- Also listen for proximity prompts automatically
        ProximityPromptService.PromptShown:Connect(function(prompt)
            if autoGrabEnabled and isAllowed then
                prompt.HoldDuration = 0
                pcall(function()
                    if fireproximityprompt then 
                        fireproximityprompt(prompt) 
                    else
                        prompt:InputHoldBegin()
                        prompt:InputHoldEnd()
                    end
                end)
            end
        end)
        
        ProximityPromptService.PromptButtonHoldBegan:Connect(function(prompt)
            if autoGrabEnabled and isAllowed then
                prompt.HoldDuration = 0
                pcall(function()
                    if fireproximityprompt then 
                        fireproximityprompt(prompt) 
                    else
                        prompt:InputHoldBegin()
                        prompt:InputHoldEnd()
                    end
                end)
            end
        end)
        
    else
        ExecuteButton.BackgroundColor3 = Color3.fromRGB(0, 100, 0)
        ExecuteButton.Text = "⚡ TOGGLE AUTOGrab ⚡"
        StatusLabel.Text = "Status: Auto Grab OFF\nPress Z to toggle"
        Outline.Color = Color3.fromRGB(0, 255, 0)
    end
end)

-- Z key toggle for Auto Grab
UserInputService.InputBegan:Connect(function(input, processed)
    if not processed and input.KeyCode == Enum.KeyCode.Z then
        if not isAllowed then
            StatusLabel.Text = "NOT WHITELISTED\nContact PetCenneti_X"
            return
        end
        
        autoGrabEnabled = not autoGrabEnabled
        
        if autoGrabEnabled then
            ExecuteButton.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
            ExecuteButton.Text = "⚡ AUTOGrab ON ⚡"
            StatusLabel.Text = "Status: Auto Grab ACTIVE\nGrabbing everything automatically"
            Outline.Color = Color3.fromRGB(0, 255, 150)
            
            -- Start auto grab loop
            spawn(function()
                while autoGrabEnabled and isAllowed do
                    grabAllProximity()
                    wait(0.3)
                end
            end)
        else
            ExecuteButton.BackgroundColor3 = Color3.fromRGB(0, 100, 0)
            ExecuteButton.Text = "⚡ TOGGLE AUTOGrab ⚡"
            StatusLabel.Text = "Status: Auto Grab OFF\nPress Z to toggle"
            Outline.Color = Color3.fromRGB(0, 255, 0)
        end
    end
end)

-- Platform-specific instructions
if isMobile then
    StatusLabel.Text = StatusLabel.Text.."\n(Mobile detected - manual clicks needed)"
else
    StatusLabel.Text = StatusLabel.Text.."\n(Press CTRL to hide/show UI)"
    if isOwner then
        StatusLabel.Text = StatusLabel.Text.."\n(Press Y for Whitelist Manager)"
    end
end

print("[DinoScripts] Auto Grab Ultimate loaded successfully!")
print("[DinoScripts] Platform: "..platform)
print("[DinoScripts] Account Status: "..(isOwner and "OWNER" or (isAllowed and "WHITELISTED" or "NOT WHITELISTED")))
print("[DinoScripts] Made by Dino")
print("[DinoScripts] Owner: PetCenneti_X")
print("[DinoScripts] Press Z to toggle Auto Grab")
if isOwner then
    print("[DinoScripts] Press Y for Whitelist Manager")
end
